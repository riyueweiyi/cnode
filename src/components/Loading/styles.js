
const styles = _ => ({
  wrapper: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    flexCirection: 'column',
    height: 'calc(100vh - 5px)'
  }
})

export default styles